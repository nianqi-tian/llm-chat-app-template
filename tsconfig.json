{
    "compilerOptions": {
        /* Visit https://aka.ms/tsconfig.json to read more about this file */
        
        // --- 1. 目标环境 (Targeting) ---
        
        /* 设置发出的 JavaScript 的语言版本，并包含兼容的库声明。*/
        "target": "es2021", 
        /* 指定一组捆绑的库声明文件，描述目标运行时环境。*/
        "lib": ["es2021"], 
        /* 指定生成的 JSX 代码 (如果您的 Worker 或前端使用了 React/JSX)。
           此处设置为 'react-jsx'，表示使用新的 JSX 转换模式。*/
        "jsx": "react-jsx", 
        
        // --- 2. 模块系统 (Modules) ---
        
        /* 指定生成的模块代码类型。es2022 支持顶级 await，适用于 Worker。*/
        "module": "es2022", 
        /* 指定 TypeScript 如何从给定的模块说明符查找文件。
           'Bundler' 模式优化了现代打包工具（如 Wrangler/Esbuild）的模块解析。*/
        "moduleResolution": "Bundler", 
        /* 启用导入 .json 文件 */
        "resolveJsonModule": true, 
        
        // --- 3. JavaScript/代码检查 ---
        
        /* 允许 JavaScript 文件成为程序的一部分。*/
        "allowJs": true, 
        /* 启用对类型检查的 JavaScript 文件中的错误报告。
           这里设置为 false，表示仅检查 TypeScript 文件。*/
        "checkJs": false, 
        
        // --- 4. 输出 (Emit) ---
        
        /* 禁止发出文件（即不生成 .js 文件）。
           这通常用于由像 Wrangler/Esbuild 这样的外部打包工具负责构建的场景。*/
        "noEmit": true, 
        
        // --- 5. 严格性和一致性 (Strictness & Consistency) ---
        
        /* 确保每个文件都可以安全地进行转译，而无需依赖其他导入。
           这对于 Workers/Bundler 环境非常重要。*/
        "isolatedModules": true, 
        /* 当模块没有默认导出时，允许 'import x from y' 语法。*/
        "allowSyntheticDefaultImports": true, 
        /* 确保导入中引用的文件大小写正确。*/
        "forceConsistentCasingInFileNames": true, 
        
        /* 启用所有严格类型检查选项 (推荐做法，以确保代码健壮性)。*/
        "strict": true, 
        
        // --- 6. 杂项 (Misc) ---
        
        /* 跳过所有 .d.ts 文件的类型检查。可以加快编译速度。*/
        "skipLibCheck": true, 
        /* 明确包含的类型声明文件。
           - ./worker-configuration.d.ts: Worker 绑定的自动生成类型。
           - node: 启用 Node.js 相关的全局类型 (与 Wrangler.toml 中的 nodejs_compat 兼容)。*/
        "types": ["./worker-configuration.d.ts", "node"]
    },
    
    // --- 7. 文件包含/排除 (Includes/Excludes) ---
    
    /* 排除的文件夹或文件列表。*/
    "exclude": ["test"],
    
    /* 包含的源文件列表。*/
    "include": ["worker-configuration.d.ts", "src/**/*.ts"]
}